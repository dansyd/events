<% if @current_user.present? %>
  <% if event.attending_status(@current_user).present? %>
    <%= button_tag event.attending_status(@current_user)%>
  <% else %>
    <%= button_tag "Not attending" %>
  <% end  %>
<% end %>
